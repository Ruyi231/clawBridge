projects:
  - id: bridge-dev
    name: ClawBridge development
    # Relative paths are resolved from the repository root, so this remains valid after cloning elsewhere.
    rootPath: .
    enabled: true
